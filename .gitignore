# Dependencies
frontend/node_modules/
scripts/node_modules/
circuits/node_modules/

# Claude Code local settings
.claude/

# Foundry build artifacts (regenerate with `forge build`)
contracts/out/
contracts/cache/

# Foundry dependencies (regenerate with `forge install`)
contracts/lib/

# Circuit trusted setup files — large, download separately
circuits/build/pot15_*.ptau

# Intermediate zkey (only the final is needed)
circuits/build/nft_ownership_0000.zkey

# Circom JS witness generator (regenerated by build-circuit.sh)
circuits/build/nft_ownership_js/

# Frontend build output
frontend/dist/
frontend/tsconfig.tsbuildinfo

# Misc
.DS_Store

# Environment files — never commit secrets
.env
.env.local
frontend/.env.local
frontend/.env.*.local
